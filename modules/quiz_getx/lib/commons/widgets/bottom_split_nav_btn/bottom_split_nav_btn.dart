import 'package:flutter/material.dart';

class BottomSplitNavBtn extends StatelessWidget {
  const BottomSplitNavBtn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
