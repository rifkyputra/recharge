import 'package:flutter/material.dart';

class ListTileSecondary extends StatelessWidget {
  const ListTileSecondary({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
