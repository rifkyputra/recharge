import 'package:flutter/material.dart';

class AppBarBrightBg extends StatelessWidget {
  const AppBarBrightBg({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
