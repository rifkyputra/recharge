import 'package:flutter/material.dart';

class AppBarDarkBg extends StatelessWidget {
  const AppBarDarkBg({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
