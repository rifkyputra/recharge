import 'package:flutter/material.dart';

class ListTilePrimary extends StatelessWidget {
  const ListTilePrimary({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
